[project]
name = "core"
version = "0.1.0"
authors = [
    { name = "ethan-wickstrom", email = "e.t.wickstrom@wustl.edu" }
]
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
]

[build-system]
requires = ["uv_build>=0.10.12,<0.11.0"]
build-backend = "uv_build"
